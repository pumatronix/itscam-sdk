# ITSCAM Client SDK -- C# / .NET API reference

This site is generated from the XML docs of the
[`Pumatronix.Itscam.Sdk`](https://www.nuget.org/packages/Pumatronix.Itscam.Sdk)
NuGet package.

For the integration guide and usage patterns, see the
[main docs site](/itscam-sdk/wrappers/csharp).

The reference focuses on the public surface; helper namespaces such as
`Pumatronix.Itscam.Native` (P/Invoke) and `Pumatronix.Itscam.RestTypes`
(generated POCOs) are filtered out so the index matches the supported
public API.
